#include <stdio.h>

/*
 * main - fn tkes pointer to int as parameta & updates de value it points to 98
 *
 * Return: Always 0;
 *
 */


int _putchar(char n);

int main(void)

{
	char n = '98';

	_putchar(n);
	return (0);
}
